for П in range(1, 10):  # П не может быть 0
    for А in range(0, 10):
        for Р in range(0, 10):
            # Проверяем, что все цифры различны
            if len({П, А, Р}) == 3:
                # Вычисляем значение выражения (П + А + Р)^4
                left_side = (Р + А + Д) ** 4
                # Формируем число ПАРИЖ, используя цифры
                right_side = int(f"{П}{А}{Р}{И}{Ж}")
                # Сравниваем результаты
                if left_side == right_side:
                    print(f"Р = {П}, А = {А}, Р = {Р}")
